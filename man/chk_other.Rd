% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-any.R, R/chk-docs.R, R/chk-manual.R,
%   R/chk-rlang.R
\name{chk_any}
\alias{chk_any}
\alias{chk_other}
\alias{chk_class}
\alias{chk_true}
\alias{chk_that}
\alias{chk_match}
\alias{chk_dots_empty}
\title{Various other check functions}
\usage{
chk_any(...)

chk_class(x, classes, ..., null.ok = FALSE, ordered = FALSE)

chk_true(x, ..., na.ok = FALSE)

chk_that(x, expr, ..., na.ok = FALSE, .varnames = ".")

chk_match(x, values = NULL, ..., multiple = FALSE, error_arg = NULL)

chk_dots_empty()
}
\arguments{
\item{...}{These dots are for future extensions and must be empty.}

\item{x}{Value to check and match. A string, or the untouched default of the
argument being matched.}

\item{classes}{Character vector of class names \code{x} must inherit from.}

\item{null.ok}{Is \code{NULL} permitted?}

\item{ordered}{Must \code{classes} appear in that order at the head of
\code{class(x)}?}

\item{na.ok}{Are missing values permitted?}

\item{expr}{Expression to evaluate on \code{x}, which is bound to \code{.} unless
\code{.varnames} says otherwise.}

\item{.varnames}{Names to bind \code{x} to when evaluating \code{expr}.}

\item{values}{Permitted values, as a character vector. \code{NULL} reads them
from the default of the caller's formal named by \code{x}, which then has to
be a symbol.}

\item{multiple}{Is \code{x} allowed to hold several values? Each must then match,
and all are returned.}

\item{error_arg}{Name to report the failure against, in place of the
expression \code{x} was written as.}
}
\value{
The original object if the check passes. \code{chk_match()} returns the
matched value visibly, \code{chk_dots_empty()} returns \code{NULL} invisibly, and
\code{chk_any()} returns the value of the first argument that passes, which is
the object that was checked.

\code{chk_match()} returns the matched value; \code{chk_dots_empty()} returns
\code{NULL} invisibly.
}
\description{
Various checks for classes and other, together
with the two \link[rlang:rlang]{rlang} assertions carried under the \code{chk_} name. See
\link{chk_atomic} for the scalar and vector types.\tabular{ll}{
   \strong{Function} \tab \strong{Passes when} \cr
   \code{chk_class(x, classes)} \tab \code{x} inherits from every class in \code{classes} \cr
   \code{chk_true(x)} \tab \code{x} is \code{TRUE} \cr
   \code{chk_that(x, expr)} \tab \code{expr}, with \code{.} bound to \code{x}, is \code{TRUE} \cr
   \code{chk_dots_empty()} \tab nothing was passed through \code{...} \cr
   \code{chk_match(arg)} \tab \code{arg} matches one of \code{values} \cr
   \code{chk_any(...)} \tab at least one of the checks given passes \cr
}


\code{chk_true()} is the catch-all: any property of any object that can be written
as a condition, at the cost of a message that can only report that the
condition was not met. \code{chk_that()} also checks that an expression is true,
but separates the value to be checked (\code{x}) from the expression evaluated on
it (\code{expr}), which makes an arbitrary condition usable on an object passing
through a pipe.

\code{chk_dots_empty()} is equivalent to \code{\link[rlang:check_dots_empty]{rlang::check_dots_empty()}} and
\code{chk_match()} is equivalent to \code{\link[rlang:arg_match]{rlang::arg_match()}}. Like every check here
\code{chk_match()} returns its input, but visibly rather than invisibly, so it is
written as \code{type <- chk_match(type)}. Its \code{arg} must be a symbol standing for
a string, not a string literal.

Two assertions for the use cases of \code{rlang::arg_match()} and \code{rlang::check_dots_empty()}.
}
\details{
\code{chk_any()} evaluates its arguments in turn and returns the value of the
first that passes. If none pass, it raises one error reporting every
failure. It is how a composite requirement is written, where each individual
\verb{chk_*()} function states only one thing:

\if{html}{\out{<div class="sourceCode">}}\preformatted{chk_any(chk_string(x), chk_number(x))
}\if{html}{\out{</div>}}

Only the checks \code{chk_any()} calls itself are candidates. One reached through
a helper function, or from inside a lambda passed to \code{lapply()}, throws where
it stands, and so does everything that is not a failed check: a misspelled
function, an argument that does not exist, an object that was never bound.

The arguments are captured as expressions and evaluated in the calling
environment, which rules out two ways of reaching \code{chk_any()} indirectly.
\code{...} cannot be forwarded into it from another function, and an object cannot
be piped into it. Both raise an error rather than being accommodated, since
the first would evaluate the checks in the wrong scope and the second would
return the piped object as a branch that passed. Write the checks at the call
site, naming the object in each.

\itemize{
\item \code{chk_match()} looks up \code{x} from \code{values}, which default to the values in
the caller's own formals, and fails if it is not matched. To assign a
default value to arg, call with \code{arg <- chk_match(arg)}. If \code{values} is
not given, \code{x} must be a symbol, since the values are then read from the
formal of that name.
\item \code{chk_dots_empty()} fails if anything was passed through \code{...}.
}
}
\examples{
# Any property that can be written as a condition
chk_true(nrow(mtcars) > 10)
mtcars |> chk_that(nrow(.) > 10) |> ncol()

# Match an argument against the values in its own default
plot_kind <- function(kind = c("scatter", "line", "bar")) chk_match(kind)
plot_kind()
tryCatch(plot_kind("pie"), error = wrap_error)

# chk_any() takes the first check that passes
x <- "a"
chk_any(chk_string(x), chk_number(x))
tryCatch(chk_any(chk_number(x), chk_logical(x)), error = wrap_error)

}
\seealso{
\link{chk_atomic} for the scalar and vector types, and \link{chk_composite}
for lists and composite objects.

\link{chk_atomic} and \link{chk_composite}.
}
