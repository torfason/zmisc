% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-any.R
\name{chk_any}
\alias{chk_any}
\title{Assert that at least one of several assertions passes}
\usage{
chk_any(...)
}
\arguments{
\item{...}{Assertion calls. Evaluated left to right, stopping at the first
that passes. Must not be named.}
}
\value{
The value of the first argument that passes, invisibly. This is the
object that was asserted on, so \code{chk_any()} can be used inline the same way
the individual assertions can.
}
\description{
\code{chk_any()} evaluates its arguments in turn and returns the value of the
first assertion that passes. If none pass, it raises one error reporting
every failure. It is how a composite requirement is written, where each
\verb{chk_*()} function states only one thing:

\if{html}{\out{<div class="sourceCode">}}\preformatted{chk_any(chk_string(x), chk_number(x))
}\if{html}{\out{</div>}}
}
\details{
Only the assertions \code{chk_any()} calls itself are candidates. An assertion
reached through a helper function, or from inside a lambda passed to
\code{lapply()}, throws where it stands, and so does everything that is not an
assertion failure: a misspelled function, an argument that does not exist,
an object that was never bound. That is the difference between this and
wrapping the branches in \code{\link[=tryCatch]{tryCatch()}}, which cannot tell a failed check from
a typo, and it is also why the passing case costs microseconds rather than
the milliseconds an \code{\link[rlang:abort]{rlang::abort()}} spends capturing a backtrace.

The arguments are captured as expressions and evaluated in the calling
environment, which rules out two ways of reaching \code{chk_any()} indirectly.
\code{...} cannot be forwarded into it from another function, and an object
cannot be piped into it. Both raise an error rather than being accommodated,
since the first would evaluate the assertions in the wrong scope and the
second would return the piped object as a branch that passed. Write the
assertions at the call site, naming the object in each.
}
\examples{
x <- "a"
chk_any(chk_string(x), chk_number(x))

y <- 3
chk_any(chk_string(y), chk_number(y))

}
\seealso{
\link{checkmate_rlang} for the scalar and vector types,
\link{checkmate_rlang_other} for containers and \code{chk_true()}.
}
