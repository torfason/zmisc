% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-manual.R
\name{chk_dots_empty}
\alias{chk_dots_empty}
\alias{chk_match}
\alias{checkmate_rlang_dots}
\title{Aliases for rlang assertions}
\usage{
chk_dots_empty(
  env = caller_env(),
  error = NULL,
  call = caller_env(),
  action = abort
)

chk_match(
  arg,
  values = NULL,
  ...,
  multiple = FALSE,
  error_arg = caller_arg(arg),
  error_call = caller_env()
)
}
\arguments{
\item{env}{Environment in which to look for \code{...}.}

\item{error}{An optional error handler passed to \code{\link[rlang:try_fetch]{try_fetch()}}. Use
this e.g. to demote an error into a warning.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{action}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}}}

\item{arg}{A symbol referring to an argument accepting strings.}

\item{values}{A character vector of possible values that \code{arg} can take.}

\item{...}{These dots are for future extensions and must be empty.}

\item{multiple}{Whether \code{arg} may contain zero or several values.}

\item{error_arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{error_call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
\code{chk_match()} returns the matched value; \code{chk_dots_empty()} returns
\code{NULL} invisibly.
}
\description{
Two \link[rlang:rlang]{rlang} functions under the \code{chk_} name, for consistency with the rest
of the family.
\itemize{
\item \code{chk_dots_empty()} is \code{\link[rlang:check_dots_empty]{rlang::check_dots_empty()}}, and fails if anything
was passed through \code{...}.
\item \code{chk_match()} is \code{\link[rlang:arg_match]{rlang::arg_match()}}, and fails unless \code{arg} matches one
of \code{values}, which default to the values in the caller's own formals.
Like every assertion here it returns its input, but visibly rather than
invisibly, so it is written as \code{type <- chk_match(type)}. Note that \code{arg}
must be a symbol (variable or function argument) representing a string,
it cannot be a string literal.
}
}
\seealso{
\link{checkmate_rlang} and \link{checkmate_rlang_other}.
}
