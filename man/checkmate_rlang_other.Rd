% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-manual.R
\name{checkmate_rlang_other}
\alias{checkmate_rlang_other}
\alias{chk_environment}
\alias{chk_list}
\alias{chk_data_frame}
\alias{chk_data_table}
\alias{chk_tibble}
\alias{chk_class}
\alias{chk_true}
\alias{chk_that}
\title{Assertion functions for objects that are not atomic vectors}
\usage{
chk_environment(x, ..., null.ok = FALSE, contains = character())

chk_list(x, ..., null.ok = FALSE, length = NULL)

chk_data_frame(x, ..., null.ok = FALSE)

chk_data_table(x, ..., null.ok = FALSE)

chk_tibble(x, ..., null.ok = FALSE)

chk_class(x, classes, ..., null.ok = FALSE, ordered = FALSE)

chk_true(x, ..., na.ok = FALSE)

chk_that(x, expr, ..., na.ok = FALSE, .varnames = ".")
}
\arguments{
\item{x}{Object to check.}

\item{...}{These dots are for future extensions and must be empty.}

\item{null.ok}{Is \code{NULL} permitted?}

\item{contains}{Character vector of names that must be bound in the
environment.}

\item{length}{Permitted length. \code{NULL} for any length, a scalar for one
exact length, or a vector whose first and last elements give the minimum
and the maximum. Neither may be negative, and \code{NA} at an end, or \code{Inf} as
the maximum, means no bound there.}

\item{classes}{Character vector of class names \code{x} must inherit from.}

\item{ordered}{Must \code{classes} appear in that order at the head of
\code{class(x)}?}

\item{na.ok}{Are missing values permitted?}
}
\value{
The original object if the assertion passes.
}
\description{
Assertions for containers and for properties that no type check covers,
with \link[rlang:rlang]{rlang} style error messages. See \link{checkmate_rlang} for the scalar and
vector types.\tabular{ll}{
   \strong{Function} \tab \strong{Passes when} \cr
   \code{chk_environment(x)} \tab \code{x} is an environment \cr
   \code{chk_list(x)} \tab \code{x} is a list, and carries no class \cr
   \code{chk_data_frame(x)} \tab \code{x} is a \code{data.frame} of sound structure \cr
   \code{chk_data_table(x)} \tab \code{x} is also a \code{data.table} \cr
   \code{chk_tibble(x)} \tab \code{x} is also a \code{tbl_df} \cr
   \code{chk_class(x, classes)} \tab \code{x} inherits from every class in \code{classes} \cr
   \code{chk_true(x)} \tab \code{x} is \code{TRUE} \cr
   \code{chk_that(x, expr)} \tab \code{x} mapped to \code{.} results in \code{expr} being TRUE \cr
}


\code{chk_true()} is the catch-all: any property of any object that can be
written as a condition, at the cost of a message that can only report that
the condition was not met.

\code{chk_that()} is a variant of \code{chk_true()} that separates the value to be
checked (\code{x}) from the expression to be evaluated on it (\code{expr}). This
can be helpful when evaluating an arbitrary condition on an object passing
through a pipe.

These take far fewer arguments than their \link[checkmate:checkmate]{checkmate} counterparts. The
container checks carry no \code{attr.ok}, since a \code{data.frame} is its class and
its row names, and only \code{chk_list()} takes a \code{length}.
}
\seealso{
\link{checkmate_rlang} for the scalar and vector types.
}
