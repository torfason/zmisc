% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-aaa-docs.R, R/chk-composite.R
\name{chk_composite}
\alias{chk_composite}
\alias{chk_environment}
\alias{chk_list}
\alias{chk_data_frame}
\alias{chk_data_table}
\alias{chk_tibble}
\title{Checks for lists and composite objects}
\usage{
chk_environment(x, ..., null.ok = FALSE, contains = character())

chk_list(x, ..., null.ok = FALSE, attr.ok = "names", length = NULL)

chk_data_frame(x, ..., null.ok = FALSE)

chk_data_table(x, ..., null.ok = FALSE)

chk_tibble(x, ..., null.ok = FALSE)
}
\arguments{
\item{x}{Object to check.}

\item{...}{These dots are for future extensions and must be empty.}

\item{null.ok}{Is \code{NULL} permitted?}

\item{contains}{Character vector of names that must be bound in the
environment. Applies to \code{chk_environment()}.}

\item{attr.ok}{Which attributes \code{x} may carry beyond those intrinsic to its
type: a character vector of permitted attribute names, \code{FALSE} for none at
all, or \code{TRUE} for any. Applies to \code{chk_list()}.}

\item{length}{Permitted length. \code{NULL} for any length, a scalar for one
exact length, or a vector whose first and last elements give the minimum
and the maximum. Neither may be negative, and \code{NA} at an end, or \code{Inf} as
the maximum, means no bound there.}
}
\value{
The original object if the check passes.
}
\description{
Checks for composite objects. See \link{chk_atomic} for scalar and vector types,
and \link{chk_other} for various other checks.\tabular{ll}{
   \strong{Function} \tab \strong{Passes when} \cr
   \code{chk_environment(x)} \tab \code{x} is an environment \cr
   \code{chk_list(x)} \tab \code{x} is a list, and carries no class \cr
   \code{chk_data_frame(x)} \tab \code{x} is a \code{data.frame} of sound structure \cr
   \code{chk_data_table(x)} \tab \code{x} is also a \code{data.table} \cr
   \code{chk_tibble(x)} \tab \code{x} is also a \code{tbl_df} \cr
}
}
\examples{
chk_data_frame(mtcars)
chk_list(list(a = 1, b = 2), length = 2)

# chk_list() only accepts bare (unclassed) lists
tryCatch(chk_list(mtcars), error = wrap_error)

}
\seealso{
\link{chk_atomic} for the scalar and vector types, and \link{chk_other} for
classes, conditions, and the \link[rlang:rlang]{rlang} aliases.
}
