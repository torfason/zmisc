% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chk-docs.R, R/chk-generated.R
\name{chk_atomic}
\alias{chk_atomic}
\alias{chk_flag}
\alias{chk_logical}
\alias{chk_string}
\alias{chk_character}
\alias{chk_number}
\alias{chk_numeric}
\alias{chk_inumber}
\alias{chk_integer}
\alias{chk_dnumber}
\alias{chk_double}
\alias{chk_znumber}
\alias{chk_integerish}
\alias{chk_count}
\alias{chk_naturalish}
\alias{chk_factor}
\alias{chk_complex}
\alias{chk_raw}
\alias{chk_day}
\alias{chk_date}
\alias{chk_instant}
\alias{chk_posixct}
\alias{chk_scalar}
\title{Checks for scalars and atomic vectors}
\usage{
chk_flag(x, ..., na.ok = FALSE, null.ok = FALSE, attr.ok = "names")

chk_logical(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL
)

chk_string(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_character(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_number(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_numeric(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_inumber(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_integer(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_dnumber(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_double(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_znumber(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_integerish(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_count(
  x,
  ...,
  na.ok = FALSE,
  zero.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names"
)

chk_naturalish(
  x,
  ...,
  na.ok = TRUE,
  zero.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_factor(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL
)

chk_complex(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL
)

chk_raw(x, ..., null.ok = FALSE, attr.ok = "names", length = NULL)

chk_day(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_date(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_instant(
  x,
  ...,
  na.ok = FALSE,
  null.ok = FALSE,
  attr.ok = "names",
  range = NULL
)

chk_posixct(
  x,
  ...,
  na.ok = TRUE,
  null.ok = FALSE,
  attr.ok = "names",
  length = NULL,
  range = NULL
)

chk_scalar(x, ..., na.ok = FALSE, null.ok = FALSE, attr.ok = "names")

chk_atomic(x, ..., na.ok = TRUE, attr.ok = "names", length = NULL)
}
\arguments{
\item{x}{Object to check.}

\item{...}{These dots are for future extensions and must be empty.}

\item{na.ok}{Are missing values permitted?}

\item{null.ok}{Is \code{NULL} permitted?}

\item{attr.ok}{Which attributes \code{x} may carry beyond those intrinsic to its type: a character vector of permitted attribute names, \code{FALSE} for none at all, or \code{TRUE} for any.}

\item{length}{Permitted length. \code{NULL} for any length, a scalar for one exact length, or a vector whose first and last elements give the minimum and the maximum. Neither may be negative, and \code{NA} at an end, or \code{Inf} as the maximum, means no bound there.}

\item{range}{Permitted range of values, under the same first/last rule as \code{length}. For the character types it constrains \code{nchar()} of the elements instead, and for the date and time types the bounds are themselves \code{Date} or \code{POSIXct}. \code{NA} at an end means no bound there, and so does an infinite end wherever the type keeps that meaning.}

\item{zero.ok}{Is zero permitted?}
}
\value{
The original object if the check passes.
}
\description{
Validity checks for atomic vectors. Each function below takes the same
arguments -- \code{na.ok}, \code{null.ok}, \code{attr.ok}, and \code{length} or \code{range} where
they apply.

They are meant to be cheap enough to leave at the top of any function, a
passing value with default arguments tends to take under a microsecond on a
modern computer; with extra specification arguments it can take up to ten
microseconds. Assembling a good message happens on the failing path, which
runs once and then stops.\tabular{lll}{
   \strong{R Type} \tab \strong{Scalar} \tab \strong{Vector} \cr
   Any type \tab \code{chk_scalar(x)} \tab \code{chk_atomic(x)} \cr
   \code{logical} \tab \code{chk_flag(x)} \tab \code{chk_logical(x)} \cr
   \code{character} \tab \code{chk_string(x)} \tab \code{chk_character(x)} \cr
   \code{numeric} \tab \code{chk_number(x)} \tab \code{chk_numeric(x)} \cr
   \code{integer} \tab \code{chk_inumber(x)}⁴ \tab \code{chk_integer(x)} \cr
   \code{double} \tab \code{chk_dnumber(x)}⁴ \tab \code{chk_double(x)} \cr
   \code{integerish}¹ \tab \code{chk_znumber(x)} \tab \code{chk_integerish(x)} \cr
   \code{naturalish}² \tab \code{chk_count(x)} \tab \code{chk_naturalish(x)} \cr
   \code{factor} \tab ³ \tab \code{chk_factor(x)} \cr
   \code{complex} \tab ³ \tab \code{chk_complex(x)} \cr
   \code{raw} \tab ³ \tab \code{chk_raw(x)} \cr
   \code{Date} \tab \code{chk_day(x)} \tab \code{chk_date(x)} \cr
   \code{POSIXct} \tab \code{chk_instant(x)} \tab \code{chk_posixct(x)} \cr
}

\itemize{
\item ¹ \code{integerish} refers to functional integers (numbers that are very close
to integer values), regardless of type (\code{integer} or \code{double})
\item ² \code{naturalish} refers to functional integers restricted to the natural
numbers (zero and positive numbers)
\item ³ No check functions are provided for scalar \code{factor}, \code{complex}, or \code{raw}
}
}
\examples{
# A check returns its input (invisibly), so it composes in a pipe
c(2, 4, 6) |> chk_numeric(length = 3) |> sum()

# One parameter set across the whole family
chk_string("abc", range = c(1, 3))
chk_integer(1:5, length = c(1, 10))

# On failure, the error names the argument as the caller wrote it
my_mean <- function(x) {
  chk_numeric(x)
  sum(x) / length(x)
}
tryCatch(my_mean("seven"), error = wrap_error)

}
\seealso{
\link{chk_composite} for lists and list-based objects, and \link{chk_other}
for classes, conditions, and various other checks
}
